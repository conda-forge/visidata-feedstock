{% set name = "visidata" %}
{% set version = "1.3.1" %}
{% set sha256 = "2b2fc55c6c25d2d0b60687a898fe78c6590df822e4927252d6b093085b3e7bea" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python >=3.4
    - pip
    - setuptools
  run:
    - python >=3.4
    - python-dateutil
    - requests
    - lxml
    - openpyxl
    - xlrd

test:
  commands:
    - vd --version

about:
  home: https://github.com/saulpw/visidata
  license: GPL-3.0
  license_family: GPL
  license_file: '{{ environ["RECIPE_DIR"] }}/LICENSE'
  summary: 'A terminal interface for exploring and arranging tabular data.'

  description: |
    VisiData is a multipurpose terminal utility for exploring, cleaning,
    restructuring and analysing tabular data. Current supported sources are
    TSV, CSV, fixed-width text, JSON, SQLite, HTTP, HTML, .xls, and .xlsx
    (Microsoft Excel).
  doc_url: https://visidata.org/docs
  dev_url: https://github.com/saulpw/visidata

extra:
  recipe-maintainers:
    - anjakefala
